package de.myhornets.rise1.store

/**
 * Platzhalter, damit `:store` einen Quellbaum hat und der Modulschnitt baubar
 * ist — dieselbe Rolle wie in den übrigen noch leeren Modulen.
 *
 * Ersetzt durch den ersten echten Inhalt: `RiseDatabase` mit den Entities aus
 * TDD 4.3 und 4.4 (`T-020`, `T-023`).
 *
 * Das Modul selbst ist mit [[ADR-003]] entschieden. Dass es hier zunächst leer
 * steht, ist Absicht: Ein zehntes Modul lässt `verifyModuleBoundaries`
 * fehlschlagen, und dieser Wächter soll einmal grün gesehen werden, bevor
 * Entities darauf gebaut werden.
 */
internal object Placeholder
